= Testkonzept

== Testobjekte und Teststufen Die Tests orientieren sich an der klassischen Testpyramide und decken mehrere Teststufen ab:

* Modultests:
API-Endpunkte und Funktionen mit gemockter Datenbank

* Integrationstests:
Zusammensoiel von API, Routing, HTTP-Statuscodes mit gemockter Datenbank

* Systemtests:
Zusammenspiel von Frontend und Backend aus Anwendersicht, manuelles Testen

== Testmethoden

=== Backend Für das Backend werden automatisierte Tests eingesetzt:

pytest als Testframework

unittest.mock (MagicMock, patch) zur Simulation der Datenbankschicht

FastAPI TestClient zur Simulation von HTTP-Anfragen

Swagger UI zur manuellen Überprüfung und Exploration der API-Endpunkte

Die Datenbank wird in den Tests vollständig gemockt, sodass keine echte Verbindung zu einer produktiven oder lokalen Datenbank notwendig ist. Dadurch bleiben die Tests isoliert, reproduzierbar und unabhängig von der Laufzeitumgebung (z. B. VM).

=== Frontend das Frontend wird durch manuelle Tests überprüft:

Das UI kann lokal mit `npm run dev` im Browser gestartet werden.
Dafür wird ein ".env.development" mit folgender Struktur benötigt.
Diesen bekommt man von einem aktiven Entwickler.

[source]
----
VITE_API_BASE_URL="..."
----

Es sollte sowhol das spezifische Feature der Pull-Request erprobt werden,
als auch die allgemeine Funktionalität der Anwendung.
Teste wie ein Nutzer die Oberfläche sieht und bedient,
wie Fehlermeldungen wirken und ob sowhol Design als auch Funktionalität aus Nutzersicht passen.

== Testdurchführungsplanung

* Wann:
** Backend-Tests: kontinuierlich während der Entwicklung, automatisiert bei jedem Commit/Push
** Frontend-Tests: Dev Team: nach Änderungen an der Oberfläche und Funktionalität beim Review des Pull-Requestes bzw während der Entwicklung
PO unregelmäßig bei Überpfüfen der gestellten Anforderungen

* Wie:
** Backend: automatisiert über `python -m pytest tests_api.py`
** Frontend: manuell im Browser mit "durchklicken"
